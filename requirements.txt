# Requirements for this project
# Notes:
# - These are the top-level libraries actually imported by the code and tests.
# - All pins are the latest stable releases as of 2025-09-28.
# - Python 3.9+ is required by sentence-transformers and tree-sitter-language-pack.

# Core runtime deps
numpy==2.1.0
sentence-transformers==5.1.1
transformers==4.51.0
tree-sitter==0.23.2
tree-sitter-language-pack==0.9.1
einops==0.8.0
SQLAlchemy==2.0.43
sqlalchemy-libsql==0.2.0
psycopg2-binary==2.9.11
pgvector==0.4.2
# Dev / tests
# CPU-only baseline: do not add flash_attn or other GPU-only attention dependencies here.
pytest==8.4.2
coverage==7.13.4
pytest-cov==7.0.0
fastmcp==2.14.5
uvicorn==0.40.0
